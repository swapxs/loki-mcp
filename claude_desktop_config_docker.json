{
  "mcpServers": {
    "calculator": {
      "command": "docker",
      "args": ["run", "--rm", "-i", "loki-mcp-server:latest"],
      "env": {},
      "disabled": false,
      "autoApprove": ["calculate"]
    }
  }
}
